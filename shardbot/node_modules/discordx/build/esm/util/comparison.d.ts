import type { ApplicationCommand } from "discord.js";
import type { DApplicationCommand } from "../decorators/index.js";
/**
 * Recursively match field
 *
 * @param object
 * @param keys
 * @param onMatch
 */
export declare function RecursivelyMatchField(object: Record<string, any>, keys: string[], onMatch: (object: any, key: string) => void): void;
/**
 * Check if ApplicationCommand and DApplicationCommand has same properties
 *
 * @param findCommand
 * @param DCommand
 * @param isGuild
 * @returns
 */
export declare function isApplicationCommandEqual(findCommand: ApplicationCommand, DCommand: DApplicationCommand, isGuild?: true): boolean;
