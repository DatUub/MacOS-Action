import type { ButtonInteraction, CacheType, InteractionCollector, StringSelectMenuInteraction, TextBasedChannel } from "discord.js";
import { Message } from "discord.js";
import type { PaginationResolver } from "./Resolver.js";
import type { IGeneratePage, PaginationInteractions, PaginationItem, PaginationOptions } from "./types.js";
export declare class Pagination<T extends PaginationResolver = PaginationResolver> {
    sendTo: PaginationInteractions | Message | TextBasedChannel;
    pages: PaginationItem[] | T;
    maxLength: number;
    currentPage: number;
    option: PaginationOptions;
    collector?: InteractionCollector<ButtonInteraction<CacheType> | StringSelectMenuInteraction<CacheType>>;
    message?: Message;
    private _isSent;
    private _isFollowUp;
    get isSent(): boolean;
    constructor(sendTo: PaginationInteractions | Message | TextBasedChannel, pages: PaginationItem[] | T, config?: PaginationOptions);
    /**
     * Unable to update pagination error
     */
    private unableToUpdate;
    /**
     * Get page
     *
     * @param page
     *
     * @returns
     */
    getPage: (page: number) => Promise<IGeneratePage | undefined>;
    /**
     * Send pagination
     * @returns
     */
    send(): Promise<{
        collector: InteractionCollector<ButtonInteraction<CacheType> | StringSelectMenuInteraction<CacheType>>;
        message: Message;
    }>;
}
