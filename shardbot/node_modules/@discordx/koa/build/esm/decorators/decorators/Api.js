import { Modifier } from "@discordx/internal";
import { DRouter, MetadataStorage } from "../../index.js";
import { DRequest } from "../index.js";
export function Api(name) {
    return function (target, key, descriptor) {
        MetadataStorage.instance.addModifier(Modifier.create((original) => {
            original.api = name;
        }, DRouter, DRequest).decorateUnknown(target, key, descriptor));
    };
}
//# sourceMappingURL=Api.js.map